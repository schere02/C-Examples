#pragma warning(disable:4996)
#include <stdio.h>

void main() {
    int num;

    printf("enter a number: ");
    scanf("%d", &num);

    //음수와 양수를 판별
    if (/*코드작성*/) printf("number is negative\n");
    else printf("numver is positive\n");
};